version: 1
services:
  - type: web
    name: gr0k-dm-migrate        # ← this can stay with hyphen, no problem
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: npx drizzle-kit push:pg
    envVars:
      DATABASE_URL: postgresql://gr0k_dm_db_user:GxPuNrlcUlfZEHImr8m7bMu6B3NuBbdz@dpg-d4ra7i7pm1nc73bjqtlg-a/gr0k_dm_db
        fromDatabase:
          name: gr0k_dm_db        # ← ONLY THIS ONE needs underscore (exact DB name)
          property: connectionString
